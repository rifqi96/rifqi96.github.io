export default {
  auth: "auth", // Subdomain auth.rifqi.dev → pages/auth/*
  console: "console", // Subdomain console.rifqi.dev → pages/console/*
  main: "_main", // Main domain (rifqi.dev) pages (could be in pages/default or at root)
};
